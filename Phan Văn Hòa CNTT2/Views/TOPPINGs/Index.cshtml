@model IEnumerable<Phan_Văn_Hòa_CNTT2.Models.TOPPING>

@{
    ViewBag.Title = "Index";
}

<h2 class="QTV3">Topping </h2>

<p class="QTV3">
    @Html.ActionLink("Thêm Topping", "Create")
</p>
<table class="table1">
    <tr>
        <th>
            <p>Tên Topping</p>
        </th>
        <th>
            <p>Giá</p>

        </th>
        <th><p>Chức Năng</p></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td class="QTV2">
            @Html.DisplayFor(modelItem => item.TEN_TOPPING)
        </td>
        <td class="QTV2">
            @Html.DisplayFor(modelItem => item.GIA_TOPPING)
        </td>
        <td class="QTV1">
            @Html.ActionLink("Xem", "Details", new { id = item.MA_TOPPING }, new { @class = "btn-custom btn-view" }) |
            @Html.ActionLink("Sửa", "Edit", new { id = item.MA_TOPPING }, new { @class = "btn-custom btn-edit" }) |
            @Html.ActionLink("Xóa", "Delete", new { id = item.MA_TOPPING }, new { @class = "btn-custom btn-delete" })
        </td>
    </tr>
}

</table>
